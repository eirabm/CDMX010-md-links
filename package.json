{
  "name": "md-links",
  "version": "0.1.0",
  "dependencies": {
    "chalk": "^4.1.0",
    "marked": "^2.0.1",
    "node-fetch": "^2.6.1"
  },
  
  "devDependencies": {
    "eslint": "^7.23.0",
    "jest": "^26.6.3"
  },
  "scripts": {
    "eslint": "eslint --ext .js ./ test/",
    "pretest": "npm run eslint",
    "test": "jest --coverage"  }
}
